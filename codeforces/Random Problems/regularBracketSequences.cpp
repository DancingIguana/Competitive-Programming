#include <iostream>
#include <vector>
#include <string>

using namespace std;
int gn;

vector<string> build(int n, int current) {
	
	if(n == 1){
		return "()";
	}
	if (current == gn) return ""
	
}

int main() {
	
	int n;
	cin >> n;
	gn = n;
	return 0;
}
