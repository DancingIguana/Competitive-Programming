#include <iostream>

using namespace std;

#define ll long long

int main() {
	ll n;
	ll numbers[n];
	for(ll i = 0; i < n; i++) {
		cin >> numbers[i];
	}
	sort(numbers, numbers+n);
	ll sum = 0,
	for()
	return 0;
}
